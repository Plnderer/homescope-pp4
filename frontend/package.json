{
  "name": "homescope-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"require(require('path').join(process.env.INIT_CWD || process.cwd(), 'scripts/vite-runner.cjs'))\"",
    "build": "node -e \"require(require('path').join(process.env.INIT_CWD || process.cwd(), 'scripts/vite-runner.cjs'))\"",
    "preview": "node -e \"require(require('path').join(process.env.INIT_CWD || process.cwd(), 'scripts/vite-runner.cjs'))\""
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  }
}
